import React from "react";
import styled from "styled-components";

export class TrackApp extends React.PureComponent {
  render() {
    return <StyledTrackApp className="container">Track</StyledTrackApp>;
  }
}

const StyledTrackApp = styled.div``;
